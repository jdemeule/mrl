PROJECT(mrl)

FIND_PACKAGE(GTest REQUIRED)

SET(TARGET mrl)

SET(MRL_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB **/*.hpp SOURCES)

ADD_LIBRARY(${TARGET} INTERFACE ${SOURCES})

ADD_SUBDIRECTORY(test)
